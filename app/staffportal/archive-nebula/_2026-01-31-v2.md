🌌 Snapshot: Project "Yggdrasil Portal" (Rel. 2026.01.31)
Core Architecture: Shell-Host-Card (Decoupled)

🏗️ 1. The Anchor: nebulaHost (Global ID)
Type: sap.ui.integration.Host

Registry: Managed in Component.js (Portal Core).

Context: Reactive proxy to JSONModel ui.

Interface:

getContext(): returns Promise<uiModel.getData()>.

setContext(mCtx): updates ui model + fires configurationChange.

publishEvent / subscribeEvent: PubSub bridge (The Ether Resonator).

📡 2. The Ether (Communication)
Method: EventProvider via Host.

Key Signal: nebulaTabChange (Payload: { tab: "sKey" }).

Flow: NavCard (Select) -> Host.publish -> Shell.subscribe -> _assemblePage.

🛰️ 3. The Bridge: SDK Base.controller.js
Function: Automated discovery of Host instance through ComponentData.

Discovery Key: __sapUiIntegration_card.

Mechanism: Controller gets Card -> Card gets Host -> Controller gets Ether.

🗂️ 4. Layout & Routing
Header: Fixed.

Navigation: Component Card (Tab list from ui>/currentRoleConfig/navigation).

Main Stage: galaxyCore (Dynamic container for Content Cards).

Current Status: 🟢 Stable. Navigation Forge functional. Role-based context injection verified. Race conditions resolved via configurationChange attachment.